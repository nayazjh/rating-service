server:
  port: 8082


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ratingservice
    username: root
    password: born2win
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: RATING-SERVICE
  config:
    import: optional:configserver:http://localhost:8084